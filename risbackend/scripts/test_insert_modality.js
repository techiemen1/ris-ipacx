require('dotenv').config(); const { pool } = require('../config/postgres'); (async () => { try { console.log('Inserting...'); const res = await pool.query("INSERT INTO modalities (name, ae_title, ip_address, port, description) VALUES (, , , , ) RETURNING *", ['TEST_MANUAL', 'TEST_AET', '1.1.1.1', 104, 'Manual']); console.log('Inserted:', res.rows[0]); } catch(e) { console.error(e); } finally { await pool.end(); } })();
